## Code Implementation
When implementing code, always follow a TDD approach.
- One good test is better than 10 bad ones.

## Command Line
- `uv` is the build backend and package manager.
- Always use `uv run` over `.venv/Scripts/python.exe` or `python`.