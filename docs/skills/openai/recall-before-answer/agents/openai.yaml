interface:
  display_name: Recall before answer
  short_description: Search vault notes before answering domain questions

dependencies:
  tools:
    - type: mcp
      description: obsidian-memory-mcp server (>= v0.2.0) with search_notes, read_note, read_section
